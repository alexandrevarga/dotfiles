# Login shell environment (sourced before .zshrc on login shells).
# PATH and env vars are managed exclusively in .zshenv.
# Do NOT source .zshrc here — zsh sources it automatically for interactive shells.
